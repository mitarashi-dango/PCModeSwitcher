# PC Mode Switcher v0.3.1

## 変更内容

- マイク設定の表示を「OFF（ミュート）」「ON（ミュート解除）」へ変更
- モード適用結果に、適用したマイク設定と失敗理由を表示
- 「変更しない」のモードでも、現在のマイクがONかOFFかを結果欄に表示
- 既定のマイクを確認できない場合は、モード適用を失敗扱いにせず「現在状態を確認できません」と表示
- 長い適用結果が画面下部で折り返されるよう改善

## 更新方法

v0.3.0から実行ファイルを置き換えるだけで更新できます。保存済みのモード設定はそのまま引き継がれます。

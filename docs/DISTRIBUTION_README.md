# PC Mode Switcher 0.1.0

Windows 11の画面OFF時間、スリープ時間、電源プランを、GAME / WORK / NORMALの3モードとしてまとめて切り替えるアプリです。

## 動作環境

- Windows 11（64ビット）
- 管理者権限は不要
- .NETの追加インストールは不要

## 使い方

1. ZIPを任意のフォルダーへ展開します。
2. `PCModeSwitcher.exe` を起動します。
3. GAME / WORK / NORMALから使いたいモードの「このモードに切り替える」を押します。
4. 各モードの設定を変える場合は「設定を編集」を押します。

初期状態では右上の閉じるボタンを押すと通知領域へ格納されます。通知領域のアイコンをダブルクリックすると再表示し、右クリックメニューの「終了」で完全に終了します。この動作と格納時の通知は、アプリの「設定」で変更できます。

アプリをもう一度起動した場合は新しい画面を増やさず、すでに起動している画面を表示します。

## 設定の保存場所

設定は次のファイルへ自動保存されます。

```text
%LOCALAPPDATA%\PCModeSwitcher\settings.json
```

アプリを更新するときは、新しいZIPを展開して `PCModeSwitcher.exe` を置き換えてください。保存済みの設定はそのまま引き継がれます。

## Windowsの警告が表示された場合

この配布版にはコード署名がないため、ダウンロードしたファイルの初回起動時にWindows Defender SmartScreenの警告が表示される場合があります。入手元が公式GitHubリポジトリであることを確認したうえで実行してください。

## アンインストール

通知領域のアイコンを右クリックして「終了」を選び、展開したフォルダーを削除します。保存済みの設定も消す場合は、上記の設定ファイルを含む `%LOCALAPPDATA%\PCModeSwitcher` フォルダーを削除してください。

## 安全性

- Windows Power APIだけを使用し、レジストリは変更しません。
- 利用可能な電源プランだけを適用します。
- 画面OFFまたはスリープの設定変更が途中で失敗した場合は、その項目を変更前の値へ戻します。

不具合や要望は [GitHub Issues](https://github.com/mitarashi-dango/PCModeSwitcher/issues) へお願いします。

